const { adminsProvider } = require("./admins");
const { marketsProvider } = require("./markets");
const { ordersProvider } = require("./orders");
const { productsProvider } = require("./products");

module.exports = {
  adminsProvider,
  marketsProvider,
  ordersProvider,
  productsProvider,
};
